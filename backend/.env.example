# The Odds API key. Required: the backend reads this at startup and odds
# fetches return empty without it. Get a key at https://the-odds-api.com/
ARBFLO_ODDS_API_KEY=

# Optional: sport key to scan. Defaults to "upcoming" (all upcoming events).
# ARBFLO_SPORT_KEY=upcoming
